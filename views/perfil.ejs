<!DOCTYPE html>
<html lang="es-ES">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Mi Perfil</title>
    <script defer src="/js/bootstrap.bundle.min.js"></script>
</head>
<body>
    <header>
        <%- include('navbar') %>
    </header>

    <div class="container py-5">
        
        <div class="text-center mb-5">
            <h1 class="display-5 fw-bold mb-3">Mi Perfil</h1>
            <img src="<%= usuario.imagen || '/images/default-avatar.png' %>" 
                 alt="Foto de perfil" 
                 class="rounded-circle border border-5 border-primary shadow"
                 style="width: 160px; height: 160px; object-fit: cover;">
        </div>

        <% if (mensaje) { %>
            <div class="alert alert-success border-start border-5 border-success shadow-lg" role="alert">
                <i class="bi bi-check-circle-fill me-2"></i><%= mensaje %>
            </div>
        <% } %>
        
        <% if (error) { %>
            <div class="alert alert-danger border-start border-5 border-danger shadow-lg" role="alert">
                <i class="bi bi-exclamation-triangle-fill me-2"></i><%= error %>
            </div>
        <% } %>

        <div class="row g-4">
            
            <div class="col-md-6 col-lg-6">
                <div class="card h-100 border-0 shadow-lg">
                    <div class="card-body p-4">
                        <h4 class="card-title text-primary border-start border-4 border-primary ps-3 mb-4 fw-bold">
                            Información Básica
                        </h4>
                        
                        <form action="/perfil/actualizar" method="POST">
                            <div class="mb-3">
                                <label for="nombre" class="form-label fw-bold">Nombre Completo</label>
                                <input type="text" id="nombre" name="nombre" class="form-control"
                                       value="<%= usuario.nombre %>" required>
                            </div>

                            <div class="mb-3">
                                <label for="correo" class="form-label fw-bold">Correo Electrónico</label>
                                <input type="email" id="correo" name="correo" class="form-control"
                                       value="<%= usuario.correo %>" required>
                            </div>

                            <div class="mb-3">
                                <label for="telefono" class="form-label fw-bold">Teléfono</label>
                                <input type="tel" id="telefono" name="telefono" class="form-control"
                                       value="<%= usuario.telefono %>" required>
                            </div>

                            <div class="d-grid mt-4">
                                <button type="submit" class="btn btn-primary">Guardar Cambios</button>
                            </div>
                        </form>
                    </div>
                </div>
            </div>

            <div class="col-md-6 col-lg-6">
                <div class="card h-100 border-0 shadow-lg">
                    <div class="card-body p-4">
                        <h4 class="card-title text-primary border-start border-4 border-primary ps-3 mb-4 fw-bold">
                            Información de Cuenta
                        </h4>
                        <div class="alert alert-light border">
                            <p class="mb-2"><strong>ID de Usuario:</strong> <%= usuario.id_usuario %></p>
                            <p class="mb-2"><strong>Rol:</strong> <span class="badge bg-secondary"><%= usuario.rol %></span></p>
                            <% if (usuario.id_concesionario) { %>
                                <p class="mb-0"><strong>ID Concesionario:</strong> <%= usuario.id_concesionario %></p>
                            <% } %>
                        </div>
                        <small class="text-muted d-block text-center mt-3">
                            * Estos datos no son modificables por seguridad.
                        </small>
                    </div>
                </div>
            </div>

            <div class="col-md-6 col-lg-6">
                <div class="card h-100 border-0 shadow-lg">
                    <div class="card-body p-4">
                        <h4 class="card-title text-primary border-start border-4 border-primary ps-3 mb-4 fw-bold">
                            Cambiar Contraseña
                        </h4>
                        <form action="/perfil/cambiar-contrasena" method="POST">
                            <div class="mb-3">
                                <label for="contraseñaActual" class="form-label fw-bold">Contraseña Actual</label>
                                <input type="password" name="contraseñaActual" id="contraseñaActual" class="form-control"
                                       pattern="^(?=.*[a-z])(?=.*[A-Z])(?=.*\d).{8,}$" required>
                            </div>

                            <div class="mb-3">
                                <label for="contraseñaNueva" class="form-label fw-bold">Nueva Contraseña</label>
                                <input type="password" name="contraseñaNueva" id="contraseñaNueva" class="form-control"
                                       pattern="^(?=.*[a-z])(?=.*[A-Z])(?=.*\d).{8,}$" required>
                            </div>

                            <div class="mb-3">
                                <label for="contraseñaConfirmar" class="form-label fw-bold">Confirmar Nueva</label>
                                <input type="password" name="contraseñaConfirmar" id="contraseñaConfirmar" class="form-control"
                                       pattern="^(?=.*[a-z])(?=.*[A-Z])(?=.*\d).{8,}$" required>
                            </div>

                            <div class="d-grid mt-4">
                                <button type="submit" class="btn btn-primary">Actualizar Password</button>
                            </div>
                        </form>
                    </div>
                </div>
            </div>

            <div class="col-md-6 col-lg-6">
                <div class="card h-100 border-0 shadow-lg">
                    <div class="card-body p-4">
                        <h4 class="card-title text-primary border-start border-4 border-primary ps-3 mb-4 fw-bold">
                            Imagen de Perfil
                        </h4>
                        <form action="/perfil/actualizar-imagen" method="POST" enctype="multipart/form-data">
                            <div class="mb-3">
                                <label for="imagen" class="form-label fw-bold ">Seleccionar nueva imagen</label>
                                <input type="file" id="imagen" name="imagen" class="form-control" accept="image/*" required>
                                <div class="form-text text-muted mt-2">
                                    Formatos: JPG, PNG, GIF (máx. 5MB)
                                </div>
                            </div>
                            <div class="d-grid mt-4">
                                <button type="submit" class="btn btn-primary">
                                    Subir Imagen
                                </button>
                            </div>
                        </form>
                    </div>
                </div>
            </div>
        </div> 
    </div>

    <footer>
        <!--Importamos el footer que tenemos creado-->
        <div id="footer-container"></div>
        <script>
          fetch('/footer.html')
            .then(response => response.text())
            .then(html => {
              document.getElementById('footer-container').innerHTML = html; 
            });
        </script>
    </footer>
</body>
</html>