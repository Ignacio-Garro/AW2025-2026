<!DOCTYPE html>
<html lang="es-ES">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Vehículos</title>
    <link rel="stylesheet" href="/css/bootstrap.min.css">
    <script defer src="https://cdn.jsdelivr.net/npm/bootstrap@5.2.3/dist/js/bootstrap.bundle.min.js"></script>
    <link rel="stylesheet" href="/css/stylesReservas.css">
</head>

<body>
  <header>
      <%- include('navbar') %>
    </header>

    <div class="row w-75 mx-auto my-5"> <!-- fila de bootstrap con ancho 75% centrada, margen eje x auto - margen eje y 5-->
      
      <div class="col-md-6"> <!-- col de ancho 6 para info del coche-->
        <div class="card shadow p-4">
          <img src="<%= vehiculo.imagen %>" class="card-img-top" alt="Imagen del vehículo">
          
          <div class="card-body">
            <h3 class="card-title">
              <%= vehiculo.marca %> <%= vehiculo.modelo %>
            </h3>
            <ul class="card-text list-group list-group-flush">
              <li class="list-group-item"><strong>Autonomía:</strong> <%= vehiculo.autonomia_km %> km</li>
              <li class="list-group-item"><strong>Plazas:</strong> <%= vehiculo.numero_plazas %></li>
              <li class="list-group-item"><strong>Color:</strong> <%= vehiculo.color %></li>
              <li class="list-group-item"><strong>Año:</strong> <%= vehiculo.año_matriculacion %></li>
            </ul>
          </div>
        </div>
      </div>

      <div class="col-md-6"> <!-- col de ancho 6 para formulario reserva-->
        <div class="card p-4 shadow-sm"><!-- card con sombra-->
          <h2 class="mb-4">Confirmar Reserva</h2>

          <form action="/vehiculos/reservar" method="POST"><!-- FORM POST de reservar vehiculo -->
            
            <input type="hidden" name="id_vehiculo" value="<%= vehiculo.id_vehiculo %>"><!-- input hidden con id del vehiculo-->

            <!-- fecha recogida-->
            <div class="mb-3"> 
              <label for="fecha_inicio" class="form-label">Fecha y Hora de Recogida</label>
              <input type="datetime-local" class="form-control" name="fecha_inicio" required> <!-- form-control para los campos de entrada de datos-->
            </div>
            <!-- fecha devolucion-->
            <div class="mb-3">
              <label for="fecha_fin" class="form-label">Fecha y Hora de Devolución</label>
              <input type="datetime-local" class="form-control" name="fecha_fin" required>
            </div>

            <!-- bottones del final-->
            <div class="d-grid gap-2">
              <button type="submit" class="btn btn-success btn-lg">Confirmar Reserva</button>
              <a href="/vehiculos" class="btn btn-outline-secondary">Cancelar</a>
            </div>

          </form>
        </div>
      </div>
    </div>
  </div>

<footer>
  <!--Importamos el footer que tenemos creado-->
  <div id="footer-container"></div>
  <script>
    fetch('/footer.html')
      .then(response => response.text()) //load footer.html content
      .then(html => {
      document.getElementById('footer-container').innerHTML = html; 
    });
  </script>
</footer>
</body>