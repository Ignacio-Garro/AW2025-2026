<!DOCTYPE html>
<html lang="es-ES">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Vehículos</title>
    <link rel="stylesheet" href="/css/bootstrap.min.css">
    <script defer src="https://cdn.jsdelivr.net/npm/bootstrap@5.2.3/dist/js/bootstrap.bundle.min.js"></script>
    <link rel="stylesheet" href="#">
</head>

<body>
    <header>
        <%- include('navbar') %>
    </header>

    <div class="container my-5">
        <h2 class="mb-4">Mis Reservas</h2>

        <% if (reservas.length === 0) { %> <!--si no hay reservas-->
            <div class="alert alert-info text-center p-5 shadow-sm rounded-4"> <!--alerta azul informativo-->
                <h4>Aún no has realizado ninguna reserva</h4>
                <p class="mb-4">¡Explora nuestros Vehiculos y haz tu primera reserva!</p>
                <a href="/vehiculos" class="btn btn-primary w-50 mx-auto">Ver Vehículos Disponibles</a>
            </div>
        <% } else { %><!--Mostrar las reservas-->
            <div class="table-responsive shadow-sm rounded-4 border"><!--contenedor de la tabla responsiva con sombra, bordes redondeados y borde-->
                <table class="table table-hover align-middle mb-0"><!--tabla con hover-->

                    <thead class="table-light"> <!--cabezera de la tabla-->
                        <tr><!--table row-->
                            <th>Vehículos</th> <!--table headers -->
                            <th>Fechas</th>
                            <th>Estado</th>
                            <th>Acciones</th>
                        </tr>
                    </thead>

                    <tbody>
                        <% reservas.forEach(reserva => { %> <!--Mostrar cada reserva-->
                            <tr>
                                <td> <!--table data vehiculos -->
                                    <div class="d-flex align-items-center"> <!--d-flex para alinear horizontalmente -->
                                        <img src="<%= reserva.imagen %>" 
                                            alt="Coche" 
                                            class="rounded-3 me-4 p-1" 
                                            style="width: 200px; height: 70px; object-fit: cover;">
                                        <div>
                                            <div class="fw-bold"><%= reserva.marca %> <%= reserva.modelo %></div>
                                            <small class="text-muted"><%= reserva.matricula %></small>
                                        </div>
                                    </div>
                                </td>

                                <td> <!--table data fechas -->
                                    <small>
                                        <strong>Desde:</strong> <%= new Date(reserva.fecha_inicio).toLocaleString() %><br> <!--toLocaleString para cambiar formato al del pais-->
                                        <strong>Hasta:</strong> <%= new Date(reserva.fecha_fin).toLocaleString() %>
                                    </small>
                                </td>

                                <td> <!--table data estado -->
                                    <% if (reserva.estado === 'activa') { %>
                                        <span class="badge bg-success">Activa</span>
                                    <% } else if (reserva.estado === 'finalizada') { %>
                                        <span class="badge bg-secondary">Finalizada</span>
                                    <% } else { %>
                                        <span class="badge bg-danger"><%= reserva.estado %></span> <!--danger si cancelada-->
                                    <% } %>
                                </td>

                                <td><!--table data acciones -->
                                    <% if (reserva.estado === 'activa') { %><!--al estar activa, puede cancelarlo o finalizarlo -->
                                        
                                        <div class="d-flex gap-2">
                                            <!--botton cancelar que llama a post -->
                                            <form action="/vehiculos/cancelar" method="POST">
                                                <input type="hidden" name="id_reserva" value="<%= reserva.id_reserva %>"> <!--id hidden de la reserva -->
                                                <input type="hidden" name="id_vehiculo" value="<%= reserva.id_vehiculo %>"> <button type="submit" class="btn btn-sm btn-outline-danger">Cancelar</button>
                                            </form>
                                            <!--botton finalizar que abre modalFinalizar-->
                                            <button type="button" class="btn btn-sm btn-primary mx-3" data-bs-toggle="modal" data-bs-target="#modalFinalizar<%= reserva.id_reserva %>">Finalizar</button>
                                        </div>

                                        <!--modal de finalizar-->
                                        <div class="modal fade" id="modalFinalizar<%= reserva.id_reserva %>" tabindex="-1" aria-hidden="true"> <!--modal-->
                                            <div class="modal-dialog modal-content p-2"> <!--para posicionar y dar forma al modal-->
                                                <form action="/vehiculos/finalizar" method="POST"> <!--llamada post-->
                                                    <!--modal header-->
                                                    <div class="modal-header">
                                                        <h5 class="modal-title">Finalizar Reserva:</h5>
                                                        <button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="Close"></button><!--modal cerrar ventana-->
                                                    </div>

                                                    <!--modal body-->
                                                    <div class="modal-body">
                                                        <input type="hidden" name="id_reserva" value="<%= reserva.id_reserva %>"> <!--id hidden de la reserva -->
                                                        <input type="hidden" name="id_vehiculo" value="<%= reserva.id_vehiculo %>"> <!--id hidden de vehiculo para poder modificar km -->

                                                        <div class="mb-3">
                                                            <label class="form-label">Kilómetros recorridos (Opcional)</label>
                                                            <input type="number" name="kilometros" class="form-control">
                                                        </div>

                                                        <div class="mb-3">
                                                            <label class="form-label">Incidencias (Opcional)</label>
                                                            <textarea name="incidencias" class="form-control" rows="5"></textarea>
                                                        </div>
                                                    </div>
                                                    <!--modal footer-->
                                                    <div class="modal-footer">
                                                        <button type="button" class="btn btn-secondary" data-bs-dismiss="modal">Cerrar</button>
                                                        <button type="submit" class="btn btn-success">Confirmar Devolución</button>
                                                    </div>
                                                </form>
                                            </div>
                                        </div>

                                    <% } else { %>
                                        <span class="text-muted">-</span>
                                    <% } %>
                                </td>
                            </tr>
                        <% }) %>
                    </tbody>
                </table>
            </div>
        <% } %>
    </div>

    <footer>
      <!--Importamos el footer que tenemos creado-->
      <div id="footer-container"></div>
      <script>
        fetch('/footer.html')
          .then(response => response.text()) //load footer.html content
          .then(html => {
          document.getElementById('footer-container').innerHTML = html; 
        });
      </script>
    </footer>

</body>